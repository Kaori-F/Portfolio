document.addEventListener('DOMContentLoaded',()=>{const modal=document.getElementById('modal');if(!modal)return;const modalImage=document.getElementById('modal-image'),modalCaption=document.getElementById('modal-caption'),modalBackground=modal.querySelector('.modal-background'),galleryItems=document.querySelectorAll('.category-item');galleryItems.forEach(item=>{item.addEventListener('click',event=>{event.preventDefault();const img=item.querySelector('img'),imgSrc=img.getAttribute('src'),imgAlt=img.getAttribute('alt');modalImage.setAttribute('src',imgSrc),modalCaption.textContent=imgAlt,modal.classList.add('is-open')})});function closeModal(){modal.classList.remove('is-open')}modal.addEventListener('click',event=>{event.target===modal||event.target===modalBackground&&closeModal()}),document.addEventListener('keydown',event=>{"Escape"===event.key&&modal.classList.contains('is-open')&&closeModal()})});
